{%- if tabs.length > 1 %}
    {%- for tab in tabs %}
        <section>
            <h1>{{ tab.title }}</h1>
            {{ tab.content }}
        </section>
    {%- endfor %}
{% else %}
    {{ tabs[0].content }}
{%- endif %}
