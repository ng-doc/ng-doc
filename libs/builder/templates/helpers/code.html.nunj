{% macro render(code, language = "typescript", copyButton = true) -%}
    <pre><code class="language-{{ language | lower }}" lang="{{ language | lower }}">{{ code | escape }}</code></pre>
{%- endmacro %}
