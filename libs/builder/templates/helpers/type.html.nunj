{% macro render(type) -%}
<code indexable="false">{{ type | displayType | escape }}</code>
{%- endmacro %}

{%- macro renderNode(type) -%}
	<code indexable="false">{{ type | displayTypeNode | escape }}</code>
{%- endmacro %}
