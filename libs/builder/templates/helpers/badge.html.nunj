{%- macro render(content, attributeOverride) -%}
    <span class="ng-doc-badge" indexable="false"
          data-content="{{ attributeOverride if attributeOverride else content }}">{{ content[0] }}</span>
{%- endmacro -%}
