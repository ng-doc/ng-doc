{%- macro render(content, attributeOverride) -%}
    <span class="ng-doc-tag" indexable="false"
          data-content="{{ attributeOverride if attributeOverride else content }}">{{ content }}</span>
{%- endmacro -%}
