{
title: '<%- buildable.title %>',
route: '<%- buildable.url %>',
<% if (buildable.hasChildren) { %>
    children: [
<% buildable.children.forEach(child => { %>
			<%- include('ng-doc.navigation.ts.ejs', {buildable: child}); %>
		<% }); %>
	],
<% } %>
}
