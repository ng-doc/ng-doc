// @ts-nocheck
import {Routes} from '@angular/router';

export const NG_DOC_ROUTING: Routes = [
	{%- for buildable in buildables %}
	{
		path: '{{ buildable.route }}',
		loadChildren: () => import('{{ buildable.moduleImportPath }}').then(m => m.{{ buildable.moduleName }})
	},
	{%- endfor %}
];
